{{>header}}
<div id="car-listings">
    
    <div id="listings">

        {{#if thereAreOffers}}
           {{#each userOffers}}
             {{>car}}
           {{/each}}
        {{else}}
        <p class="no-cars">No cars in database.</p>
        {{/if}}

    </div>
</div>
{{>footer}}